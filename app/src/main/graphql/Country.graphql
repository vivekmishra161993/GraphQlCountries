query Country($country_code: ID!) {
    country(code: $country_code) {
        name
        code
        capital
        emoji
        currency
        languages {
            name
        }
        continent {
            name
        }
    }
}